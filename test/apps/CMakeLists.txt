set(NVSHMEM_TEST_PREFIX "apps_")
macro(nvshmem_add_test SOURCE)
  nvshmem_add_test_prefix(${SOURCE} ${NVSHMEM_TEST_PREFIX} ON)
endmacro()
add_subdirectory(cufft)
add_subdirectory(dgl)
if(NVSHMEM_USE_NCCL)
add_subdirectory(interop)
endif()
